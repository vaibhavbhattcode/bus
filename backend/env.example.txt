# ============================================
# BUS BOOKING PLATFORM - ENVIRONMENT CONFIG
# ============================================
# Copy this file to .env and fill in your values
# Rename this file to .env (remove .txt extension)

# ============================================
# DATABASE - MongoDB Atlas (REQUIRED)
# ============================================
# Get from: https://www.mongodb.com/cloud/atlas
# Format: mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority
DATABASE_URL="mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/bus_booking?retryWrites=true&w=majority"

# ============================================
# JWT AUTHENTICATION (REQUIRED)
# ============================================
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-min-32-chars"
JWT_EXPIRES_IN="24h"
JWT_REFRESH_SECRET="your-refresh-token-secret-change-this-in-production-min-32-chars"
JWT_REFRESH_EXPIRES_IN="7d"

# ============================================
# REDIS - For Seat Locking (OPTIONAL but recommended)
# ============================================
# Option 1: Use REDIS_URL for cloud/deployment (Redis Cloud, AWS ElastiCache, etc.)
# Format: redis://[:password@]host[:port] or rediss:// for TLS
# Example: redis://:password@redis.example.com:6379
# Example: rediss://:password@redis.example.com:6380 (TLS)
REDIS_URL=""

# Option 2: Use individual config for local Redis
REDIS_HOST="localhost"
REDIS_PORT=6379
REDIS_PASSWORD=""
REDIS_TLS="false"

# Disable Redis completely (set to false to disable Redis - seat locking will be disabled)
REDIS_ENABLED="true"

# ============================================
# SERVER CONFIGURATION (REQUIRED)
# ============================================
PORT=3000
NODE_ENV=development
APP_NAME="Bus Booking Platform"
FRONTEND_URL="http://localhost:3001"

# ============================================
# PAYMENT GATEWAY - Razorpay (OPTIONAL)
# ============================================
# Get from: https://dashboard.razorpay.com/app/keys
RAZORPAY_KEY_ID="rzp_test_xxxxxxxxxxxx"
RAZORPAY_KEY_SECRET="your_razorpay_key_secret_here"

# ============================================
# EMAIL SERVICE - SMTP (OPTIONAL)
# ============================================
# Gmail: Use App Password from https://myaccount.google.com/apppasswords
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-specific-password"

# ============================================
# FILE UPLOAD (OPTIONAL)
# ============================================
UPLOAD_PATH="./uploads"
MAX_FILE_SIZE=5242880
